#include <stdio.h>
#include <string.h>
#define MAXARGS 128
#define MAXLINE 1000

/* Function prototypes */


int main()
{
	char buf[10] = "good day";
	char *argv[] = {"Ab", "Bb"};
	
	char *bufp = buf;	

	char *delim;
	int argc;
	/* ignore leading space */	
	while (*bufp && *bifp == ' ')
		bufp++;
 	argc = 0;
	while (delim = 
	
	*argv = bufp;

	while (*bufp != ' ') {
		bufp++;
	}

	*bufp = '\0';
	bufp++;
	argv[1] = bufp;

	printf("%s", argv[0]);
	printf("%s", argv[1]);


}
